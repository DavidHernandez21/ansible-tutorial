---
- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      tags: always
      ansible.builtin.apt:
        update_cache: yes
      changed_when: false
  tasks:
    - include_tasks: 
        - tasks/packages.yaml
        - tasks/users.yaml
        - tasks/cron.yaml